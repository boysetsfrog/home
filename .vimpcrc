set expand-artists
set groupignorethe
set ignorecase
set local-music-dir /home/nathan/music
set smartcase
"set songformat {%R - %t}|{%f}$E$R $H[$H%l$H]$H
set sort library
set sortignorecase
set timeremaining
set window browse
set windownumbers

map <sc> :
map F :1browse<CR>/
map L :1library<CR>/
map @ :set add next<CR>a:set add end<CR>

map gl :load 
map ga :add 

consume on
